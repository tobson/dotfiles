" Inject everything under ~/.vim/bundle
execute pathogen#infect()

" Enable syntax highlighting
syntax on

" Detect filetype
filetype plugin indent on

" Avoid double white spaces when joining lines
set nojoinspaces

" Replace tabs with whitespace
set expandtab
