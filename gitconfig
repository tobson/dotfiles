# vim: ft=gitconfig
[user]
	name = Tobias Heinemann
	email = tobias.heinemann@gmail.com
[alias]
        ci = commit
        co = checkout
        di = diff
        st = status
        br = branch
        lg = log --graph --oneline --format='%C(bold yellow)%h %C(blue)%<(11,trunc)%an %C(white)%<(56,trunc)%s'
[color]
	ui = true
[core]
	editor = vim
	excludesfile = ~/.gitignore_global
[push]
	default = simple
